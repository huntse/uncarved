# The URL the site will be built for
base_url = "https://www.uncarved.com"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true


# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# Whether to generate a feed
generate_feeds = true

title = "The Uncarved Block"
description = "Sean Hunter's personal blog, containing software, haskell, scala, ocaml, unix tips and sundry other things."
taxonomies = [
    {name = "tags", rss = true},
]


# theme = "anpu-zola-theme"
# theme = "zola-sam"
# theme = "hyde"
[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "solarized-light"

[extra]
date_format = "%Y-%m-%d"
latex = true
theme = "toggle"
# Put all your custom variables here
